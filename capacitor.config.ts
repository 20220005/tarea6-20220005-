import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.tarea6',
  appName: '20220005Tarea6',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }

};

export default config;
