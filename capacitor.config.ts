import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.tarea6',
  appName: 'tarea6',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
